{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with Meson (MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\Muuk.exe",
            "args": ["greet"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build debug meson",
            "console": "integratedTerminal", 
            "stopAtEntry": true,
            "environment": [],
        }
    ]
}
